{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set: dialog, updater, and process plugin permissions.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:webview:default",
    "core:event:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "updater:default",
    "process:default"
  ]
}
